spring.application.name=epms


server.port=8081

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/epms}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
app.jwt.secret=${JWT_SECRET:VGhpc0lzQVN1cGVyU2VjcmV0S2V5Rm9ySldUVG9rZW5HZW5lcmF0aW9uMTIzNDU2Nzg5MDEyMzQ1Ng==}
app.jwt.access-token-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:3600000}
app.jwt.refresh-token-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}
app.onboarding.login-url=${APP_LOGIN_URL:http://localhost:5173/login}

# ===============================
# Gmail SMTP Email Configuration
# ===============================
# Never commit real credentials. Set SMTP_USER / SMTP_PASS in the environment, or
# use a local, gitignored file (see docs/SMTP_GMAIL_SETUP.md).
#
# Do NOT use your normal Gmail sign-in password. In Google Account → Security, enable
# 2-Step Verification, then create an App Password; paste that 16-character value here.
# The mailbox in SMTP_USER must exactly match the Google account (spelling, e.g. yuyu vs yuyu...).
# Common mistake: use SMTP_PASS and SMTP_USER (not STMP_*).

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SMTP_USER:yuyakhin1031999@gmail.com}
spring.mail.password=${SMTP_PASS:oihznjaxlkqhzuqv}

app.mail.from=${MAIL_FROM:EPMS HR <yuyakhin1031999@gmail.com>}

spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
